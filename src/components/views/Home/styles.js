import styled from 'styled-components';

export const StyledHome = styled.div`
  position: absolute;
  bottom: 0;
  left: 0;
  width: 100vw;
  height: 50vh;
  height: calc(var(--vh, 1vh) * 50);
  overflow: hidden;
`